import pandas as pd

# Load stops (JSON)
stops = pd.read_json('preprocessed/stops.json')  # Stays as JSON

# Load stop_times (Parquet)
stop_times = pd.read_parquet('preprocessed/stop_times.parquet')  # Uses Parquet